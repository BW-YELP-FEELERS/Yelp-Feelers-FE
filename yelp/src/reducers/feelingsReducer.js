import { FEELERS_MODE} from '../actions'

const initialState = (
    state = false
)

const feelersReducer = (state = initialState, action) => {
    switch(action.type){
        case "FEELERS_MODE":
            return
            
        }
}